meta:
  environment: node
  toolchain:
    name: browser
    version: "18"
build:
  additionalCommands: 
    - "npm install --legacy-peer-deps"
  artifacts:
    build/*: /
run:
  nginx:
    include: nginx.conf
