---
version: null
meta:
  environment: node
  toolchain:
    name: browser
    version: 18
build:
  additionalCommands:
    - cp test-index.html index.html
  artifacts:
    index.html: /index.html
run: {}
